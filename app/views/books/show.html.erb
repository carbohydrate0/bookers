<main>
  <%= notice %>
  <p class="main-text">
    <strong>Title:</strong>
      <%= @book.title %>
  </p>
  <p class="main-text">
    <strong>Body:</strong>
      <%= @book.body %>
  </p>
  
  
  <%= link_to "Edit", edit_book_path(@book) %>
  |
  <%= link_to "Back", books_path %> 
</main>
